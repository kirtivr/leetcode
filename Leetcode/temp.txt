pdb go to interactive mode: 'interactive', break out with Ctrl + D

while iter > 0:
    print(x)
    list_recipes = x
    if len(list_recipes) > 1:
        break
    if len(list_recipes) == 0:
        break
    da = x[0]
    for da_rec in da.needs_recipe: 
        if ordered_recipes.index(da_rec) > ordered_recipes.index(da):
            print(f'recipes {da} and {da_rec} are out of order')
    if list_recipes[0] in supplies:
        print(f'list_recipes[0] is satisfied')
    x = x[0].needs_recipe
